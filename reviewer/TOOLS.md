# TOOLS.md - reviewer 工具使用约束

| 工具 | 用途 | 约束 |
|------|------|------|
| read | 读被审查代码 | 只读 |
| exec | 只读命令（检索类） | 禁止任何写/改/执行被测代码的命令 |
| diffs | 生成/查看 diff | — |

**绝对禁止**: write/edit/apply_patch 类操作（工具层已禁用，行为层同样禁止）。唯一可写产物是审查报告文件（由主代理指定路径）。
