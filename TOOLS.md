# TOOLS.md - 技术环境与工具标准

## 物理开发环境

- **工作区定位**: 自适应当前运行环境的绝对路径
- **核心模型**: 支持多端调用 (企业级按量计费模式优化)

## 项目环境管理
- 各项目的独立拓扑目录（如 `apps/server/`、`mini-program/` 等）由各项目根目录下的独立说明文档定义，本全局文件仅提供底层通用工具标准。

## 活跃项目技术栈速查（2026-07 新增）

| 项目 | 前端 | 后端 | 数据库 | 部署 | 关键约束 |
|------|------|------|--------|------|----------|
| DEMAS | Tauri+React | Python | SQLite | 本地桌面 | 离线优先 |
| 螺乐好房 | 微信小程序 | Java Spring | MySQL+Redis | 腾讯云 | 高并发 |

## 硬核分析指令 (针对 >=200行代码 或 >=100MB文件)

1. **定位**: `grep -n "关键词" 文件名`
2. **提取**: `awk 'NR>=start && NR<=end' 文件名`
3. **统计**: `wc -l 文件名`

## 环境事实（2026-07-30 登记，勿再假设）

| 事实 | 值 | 踩坑记录 |
|------|-----|----------|
| Shell | **PowerShell**（非 bash） | heredoc/`&&` 连踩 3 次；多行脚本先写临时 .py 再执行 |
| 文件写入 | 中文内容**必须**用 write 工具（UTF-8） | PowerShell `Add-Content` 默认编码毁文件 |
| 配置文件批量替换 | **禁止** PowerShell `-replace`+`Set-Content` 写 openclaw.json（毁中文+BOM） | 2026-08-04 事故：用 edit 工具或 node 脚本逐块改 |
| 终端输出 | 控制台 GBK，中文文档用 read 工具读，不用 cat | 乱码返工 |
| SolidWorks | **2025 正式版**（老板确认，无到期风险） | 曾误写"试用版30天"进正式文档 |

## 测试执行纪律（2026-07-30）

| 场景 | 动作 |
|------|------|
| 小改动验证（≤5文件） | 只跑目标测试文件，禁止全套 |
| 里程碑验收 | 才跑全套（153 项 ≈3 分钟） |
| 配置变更 | 先查 schema 合法性再写入，禁止试错式改配置重启 |

## 跨技术栈数据标准
- **本地数据隔离**: 优先遵循本地离线高可用存储路径设计。
- **接口安全规范**: 凡涉及自动化通知（如 Feishu 等第三方平台 API），必须内置频率截断与去噪熔断机制。

## Token 优化指令（2026-07 新增）

| 场景 | 指令 | 效果 |
|------|------|------|
| 大文件读取 | `awk 'NR>=1 && NR<=100'` | 切片读取，避免全量加载 |
| 代码审查 | 只读取变更文件 + diff | 控制上下文 ≤ 30k |
| 项目切换 | 先归档，再加载新项目文档 | 避免上下文堆积 |
